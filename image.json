[
    {
      name: "${var.container_name}",
      image: "${format("%s:%s",  var.docker_image, var.docker_tag)}",
      cpu: 1024,
      memory: 2048,
      networkMode: "awsvpc",
      portMappings: [
        {
          containerPort: var.app_port,
          hostPort: var.app_port
        }


      ]
    }
]
